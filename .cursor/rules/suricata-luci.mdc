---
description: Suricata LuCI naming, policies, and frozen UX.
globs: feeds/luci/luci-app-suricata/**,feeds/packages/tp-eventd/**,feeds/packages/suricata/**,feeds/packages/suricata-etopen/**
alwaysApply: false
---

# Suricata LuCI

User-facing name is **Suricata** (menu JSON `title`, view `E('h2')`). Package / rpcd / CSS are `luci-app-suricata`, `luci.suricata`, `.luci-app-suricata`. Do not use `luci-app-threat-prevention`.

Do not restore **Threat Prevention** as the menu or page title.

- Vendor `.rules` are read-only; tunings in UCI `suricata.s${sid}` (do not `uci delete` the section on re-enable)
- Apply via `suricata-config-apply` → `tp-rules-apply` + `tp-policy-apply`
- Footer Save & Apply only (settings + policies). Do not add **Save policies**. Reset rulesets stays on the Policy tab.
- No Blocky/Snort header cross-links; no CM5 / 2.5 GbE copy in views
- `rpc.declare` needs `expect: { '': {} }`; buttons `'type': 'button'`
